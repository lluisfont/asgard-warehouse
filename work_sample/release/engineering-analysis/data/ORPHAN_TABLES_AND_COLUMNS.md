# Orphan Tables And Columns

Estado: INFERRED_DRAFT_REVIEW_REQUIRED
Idioma: Spanish

Auditoria funcional: tablas PHP-directas residuales relevantes = 0.

Pendientes externos/SQL-only: `con_*`, `serv_*`, `cn_*`, `bot_*` y posibles robots/procesos fuera del repo. Columnas huerfanas no se confirman sin analisis de uso por columna y runtime.
