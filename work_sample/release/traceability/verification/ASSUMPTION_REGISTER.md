# Assumption Register

Status: IN_PROGRESS

| ID | Assumption | Basis | Validation Needed |
| --- | --- | --- | --- |
| ASM-IA-001 | The reconstructed domain name is `identity-access`. | Files implement MFA, authentication, sessions, password change, and permissions. | Confirm with business owner. |
| ASM-IA-002 | The 2FA flow applies to customer users, not internal users. | `tipo_usuario = 'CLIENTE'` and updates to `dav_clienteusuarios`. | Confirm whether other user types share the flow. |
| ASM-IA-003 | Failed MFA attempt count is session scoped. | Counter uses `$_SESSION['intentos_code']`. | Confirm whether database/user/IP counters exist elsewhere. |
| ASM-IA-004 | `dav_codigos_2fa.deleted_at` is the active/inactive marker for MFA codes. | Queries filter `deleted_at is null` and updates set `deleted_at`. | Confirm cleanup and retention policy. |
| ASM-IA-005 | Graphify is supporting evidence, while source code and SQL remain the authority. | Graphify import produced graph/report; source line evidence is more direct. | Keep this rule for later domains. |
| ASM-DEO-001 | The reconstructed domain name is `document-exchange-ocr`. | Code centers on intercambio documental, exchange ids, uploaded documents and OCR validation. | Confirm naming at final review. |
| ASM-DEO-002 | IASA OCR flow is a specialized subflow inside the broader document exchange domain. | `lectura_documentos_iasa.php` is client/document specific while `iniciarIntercambio.php` is generic. | Confirm client/product boundaries. |
| ASM-DEO-003 | `intercambiodocumental` is an external or separate schema integrated with ASGARD. | Queries reference `intercambiodocumental.exchange_documents` from ASGARD code. | Confirm deployment topology. |
| ASM-CRI-001 | The reconstructed domain name is `customs-request-intake`. | Evidence centers on `SolicitudClass`, `dav_casosprevios`, document creation and request finalization. | Confirm naming at final review. |
| ASM-CRI-002 | Despacho, soporte and vehiculos are variants of the same intake lifecycle at this level. | Shared import/validation/case creation logic in `SolicitudClass`. | Split later if evidence shows separate owners or lifecycles. |
| ASM-LST-001 | The reconstructed domain name is `logistics-shipment-tracking`. | Evidence centers on logistics dashboards, `logis_embarques`, `logis_pedidos` and EDP state derivation. | Confirm naming at final review. |
| ASM-LST-002 | CBN and Alicorp dashboards are treated as client-specific views of a shared logistics tracking capability. | They share provider/EDP/dashboard structures but contain client-specific indicators. | Split later if ownership or processes diverge. |
| ASM-LQC-001 | The reconstructed domain name is `logistics-quotation-costing`. | Evidence centers on quote creation, operator cost submission and cost evaluation. | Confirm naming at final review. |
| ASM-LQC-002 | Costos by operator and internal shipment costs are related but distinct subflows within the same cost domain. | `logis_costosdetalles` stores operator quote details; `logis_costos` and `logis_costos_detalle` store categorized shipment costs. | Split later if owners differ. |
| ASM-CDA-001 | The reconstructed domain name is `customs-document-approval`. | Evidence centers on case previous documents, attachments, approval/send markers and notifications. | Confirm naming at final review. |
| ASM-CDA-002 | Document exchange/OCR and customs document approval are separate but related document domains. | `documentacion*.php` uses `dav_documentosprevios`; intercambio uses `intercambiodocumental.exchange_documents`. | Confirm ownership boundaries. |
| ASM-VIM-001 | The reconstructed domain name is `vehicle-import-management`. | Evidence centers on vehicle previous records, chassis validations, DAM gating, bulk Excel modifications and SOAT lot reassignment. | Confirm naming at final review. |
| ASM-VIM-002 | Vehicle import validation and massive vehicle modification are subflows of the same vehicle management domain. | Both flows share `dav_vehiculosprevios`, chasis, request progression and AP/DAM effects. | Split later if business ownership or lifecycle differs. |
| ASM-VIM-003 | `camposmodificar` values are interpreted from update branches: 0 means economic and item fields, 1 economic fields, 2 item fields, 3 request movement. | `VehiculosClass.php:136-156` branches by numeric value. | Confirm catalog and UI labels. |
| ASM-BPR-001 | The reconstructed domain name is `billing-payments-receivables`. | Evidence centers on factura-planilla, debit notes, received payments, receipt tracking and account statement aging. | Confirm naming at final review. |
| ASM-BPR-002 | Invoice/planilla, debit note, payment receipt and account statement views are treated as one receivables domain. | They share customer/case/accounting tables and document collection states. | Split later if accounting ownership or lifecycle differs. |
| ASM-BPR-003 | `idestadofactura = 1` and `idestadopago = 2` represent active/valid documents in inspected reporting flows. | These values are used as inclusion filters across billing and debit note reports. | Confirm state catalogs. |
| ASM-AMS-001 | The reconstructed domain name is `advisory-management-services`. | Evidence centers on `ages_*` tables, services board, requests, procedures, state lifecycle and reporting. | Confirm naming at final review. |
| ASM-AMS-002 | Advisory/support services and massive customs-management creation are related entry points in the same domain. | Both create/manage service requests or previous-case/service data and connect to documents/tramites. | Split later if business ownership differs. |
| ASM-AMS-003 | Document exchange integration is a supporting subflow of advisory services, while OCR exchange remains in `document-exchange-ocr`. | AGES stores `exchange_id` and UI creates/vinculates exchanges for service requests. | Confirm integration boundary. |
| ASM-TET-001 | The reconstructed domain name is `transport-export-tracking`. | Evidence centers on tracking trips, export reports, MIC/DEX physical reception and SCP upload. | Confirm naming at final review. |
| ASM-TET-002 | Trip tracking and export document/report controls are treated as one candidate domain because inspected UI/report flows share transport/export operational context. | `tracking/*` and `operativos/exportaciones/*` both support transport/export monitoring. | Split later if ownership differs. |
| ASM-TET-003 | MIC/DEX state names are inferred from date fields, not from a dedicated state catalog. | `RecepcionFisicaMICs.php` derives state from `fecha_recibido`, `fecha_enviado`, `fecha_concluido`. | Confirm document lifecycle. |
| ASM-MDC-001 | The reconstructed domain name is `master-data-configuration`. | Evidence centers on `parametros/*`, customer users, providers, transport operators, signers, third parties and support catalogs. | Confirm naming at final review. |
| ASM-MDC-002 | Provider, consignee, operator, agent, manager, signer and service setup are grouped as one candidate master-data domain. | The inspected parameter modules share customer-third-party-document/contact patterns. | Split later if ownership or lifecycle differs. |
| ASM-MDC-003 | Provider states 0, 1, 2 and 3 are interpreted from UI labels and query filters. | `listaProveedores.php` maps states to pendiente, por aprobar, guardado and por aprobar modificacion. | Confirm official state catalog and transitions. |
| ASM-CEC-001 | The reconstructed domain name is `certification-expiry-control`. | Evidence centers on control_certificaciones, certificate expiry, AP control, alerts and notification jobs. | Confirm naming at final review. |
| ASM-CEC-002 | Certification control and AP expiry control are grouped because both monitor regulatory documents by validity date. | `cc_registro_documentos` and `dav_autorizacionprevia` both expose expiry monitoring flows. | Split later if business ownership differs. |
| ASM-CEC-003 | Document type `3` has a special duplication rule inferred from code, but its label is not yet confirmed. | Duplicate-code validation is skipped when `tipoDocumento` is `3`. | Confirm document type catalog. |
| ASM-CIN-001 | The reconstructed domain name is `continuous-improvement-nonconformity`. | Evidence centers on `mejora-continua` views/components and `rnc_*` tables. | Confirm naming at final review. |
| ASM-CIN-002 | The modern `rnc_mejoras_continuas` flow and legacy `rnc_noconformidad` are related representations of improvement/nonconformity management. | Both schemas store hallazgo, action, responsible, risk/requisite and evidence concepts. | Confirm migration/ownership relationship. |
| ASM-CIN-003 | The API implementation for `api_url` is external or not present in inspected PHP files. | Frontend calls endpoints such as `/nueva-mejora-continua`, `/analisis`, `/verificacion`, but backend source was not located in `index_archivos`. | Locate backend routes/controllers. |
| ASM-WIR-001 | The reconstructed domain name is `warehouse-inventory-reporting`. | Evidence centers on `warehouse/*` report screens, Atlantes API calls and `inventario_*` report schema. | Confirm naming at final review. |
| ASM-WIR-002 | ASGARD is acting mainly as a reporting/integration portal for Atlantes Warehouse, not the system of record for all warehouse movements. | Report query files call `ATLANTES_API_URL` endpoints and transform responses into grids. | Confirm operational ownership with warehouse owner. |
| ASM-WIR-003 | Vehicle inventory reports are grouped with Warehouse because user-facing screens live under `warehouse` and inventory schema records chasis inspections. | `warehouse/reporte-inventario-chasis*.php` and `inventario_*` tables share chasis/inventory vocabulary. | Split later if ownership differs. |
| ASM-RTC-001 | The reconstructed domain name is `realtime-notification-center`. | Evidence centers on `servicioNotificaciones`, Pusher channels, `push_*` tables and notification dropdown actions. | Confirm naming at final review. |
| ASM-RTC-002 | State `1` is interpreted as unread/enviado and state `3` as read. | Creation code sets `idestado = 1`, counters filter `idestado = 1`, and `cambiarEstado.php` sets read to `3`. | Confirm official `push_estado` catalog. |
| ASM-RTC-003 | User-type ids are inferred from code paths: internal `1`, client `2`, operator/provider-like roles `3/4`, specific client `5`, and additional agent/operator roles `6/7`. | `verificarUsuario*`, `generaUrl`, `enviarNotificacionesID.php` branch by these numeric values. | Confirm official `push_tipousuario` catalog. |
| ASM-ORD-001 | The reconstructed domain name is `operational-reporting-downloads`. | Evidence centers on operational report screens, `LogReportes.php`, report catalog tables and massive document download. | Confirm naming at final review. |
| ASM-ORD-002 | Bulk document download belongs to reporting/download delivery, while source document lifecycle remains in `document-exchange-ocr`. | `DescargaMasivaClass.php` reads Intercambio Documental records but does not own document creation/validation lifecycle. | Confirm ownership boundary. |
| ASM-ORD-003 | `reporte_download = 0` means visualization and `1` means download/export. | Report screens call `LogReportes.php` with these values. | Confirm whether every report must apply both events. |
| ASM-SCP-001 | The reconstructed domain name is `scp-reception-control`. | Evidence centers on `scp.php`, `uploadDatosSCP.php`, `reporte-scp.php` and SCP reception tables. | Confirm naming at final review. |
| ASM-SCP-002 | SCP reception control is separate from general operational reporting because it imports and updates operational reception rows, not only displays a report. | `guardarRecepcionSCP` inserts/updates `dav_reporte_recepcion_scp`. | Confirm ownership boundary. |
| ASM-SCP-003 | `CUADRA`/`REVISAR` is interpreted as a reconciliation result between SCP sent quantity and packing-list net weight. | `getReporteSCP` computes `estado_cuadra` from `cantidad_enviada` and `peso_neto_lista_empaque`. | Confirm business tolerance and units. |
| ASM-LOS-001 | The reconstructed domain name is `logistics-order-status-milestones`. | Evidence centers on `estado-pedidos` frame/endpoints, `EstadoPedidosClass.php`, `logis_edp` and `logis_estados_edp`. | Confirm naming at final review. |
| ASM-LOS-002 | Order-status milestones are separate from shipment tracking because they are customer-visible EDP milestones attached to an order/shipment, not GPS/trip events. | `logis_edp` records EDP state history; tracking domain uses `tck_*` trip events. | Confirm ownership boundary. |
| ASM-LOS-003 | States `53`, `99` and `160` are final states because they set shipment finalization fields. | `agregarEdp` updates `logis_embarques.fecha_finalizacion` for these ids. | Confirm official state labels. |
| ASM-VTDC-001 | The reconstructed domain name is `vehicle-transitory-depot-compliance`. | Evidence centers on transitory-depot tax payment and vehicle depot reports. | Confirm naming at final review. |
| ASM-VTDC-002 | Transitory-depot compliance is separate from `warehouse-inventory-reporting` because it combines tax/depot compliance with vehicle inventory evidence. | Payment report and depot report use ASGARD vehicle/customs data plus Atlantes ingress/egress evidence. | Confirm ownership boundary. |
| ASM-VTDC-003 | The 60-day threshold is a business rule for deferred tax/payment classification. | JavaScript classifies payment as before/after 60 days and calculates deferred days after day 60. | Confirm legal/commercial source and inclusivity. |
| ASM-CGTC-001 | The reconstructed domain name is `customs-guarantee-tax-control`. | Evidence centers on boletas de garantia, DAM, tax payments, guarantee usage and tax differences. | Confirm naming at final review. |
| ASM-CGTC-002 | Guarantee/tax control is separate from `billing-payments-receivables` because it controls guarantee exposure and customs tax differences rather than customer invoicing and receivables lifecycle. | `boletasgarantia*`, `ContabilidadClass.php` and `tributosquery.php` use customs case, DAM, liquidation and requirement data. | Confirm ownership boundary. |
| ASM-CGTC-003 | Document type `4` in `cc_registro_documentos` represents guarantee/boleta records. | `getSeguimientoTotal` sums `monto_boleta` where `tipo_documento_id=4`. | Confirm document catalog. |
| ASM-CGTC-004 | The 90-day threshold is an operational threshold for non-nationalized units under guarantee exposure. | `getSeguimientoOperativo` and UI date-range validation use 90-day logic. | Confirm legal/commercial source. |
| ASM-VCAR-001 | The reconstructed domain name is `vehicle-cost-accounting-reporting`. | Evidence centers on vehicle expense, logistics cost and ZDAM accounting reports. | Confirm naming at final review. |
| ASM-VCAR-002 | Vehicle cost accounting reporting is separate from `billing-payments-receivables` and `customs-guarantee-tax-control` because it focuses on cost composition and accounting outputs by vehicle/case. | `gastosvehiculosquery.php`, `costoslogisticosvquery.php` and `reportezdamquery.php` calculate component costs and ZDAM output. | Confirm ownership boundary. |
| ASM-VCAR-003 | Client-line ids `17`, `20`, `23`, `27`, `29` and `31` represent vehicle/maquinaria lines in the inspected reports. | These ids are repeated as inclusion filters in vehicle cost queries. | Confirm line catalog. |
| ASM-VCAR-004 | Exchange rate `6.96` is the report conversion basis from Bs to USD. | UI text and formulas repeatedly use `6.96`. | Confirm accounting policy and validity period. |
| ASM-ALAR-001 | The reconstructed domain name is `accounting-ledger-aging-reporting`. | Evidence centers on aging/monthly savings, purchase ledger and invoice/planilla concept reporting. | Confirm naming at final review. |
| ASM-ALAR-002 | `agig` is interpreted as aging/ahorro monthly register because UI labels say Registro Ahorro por Mes while table name is `dav_aging`. | `agig.php` and `agig_ajax.php`. | Confirm business terminology. |
| ASM-ALAR-003 | Purchase-ledger fiscal credit is 13% of observed amount/base. | `librocomprasquery.php` calculates `ROUND(monto*0.13,2)`. | Confirm fiscal rule. |
| ASM-COKC-001 | The reconstructed domain name is `customs-operational-kpi-control`. | Evidence centers on operational KPI, control AD/OL and customs/logistics seguimiento reports. | Confirm naming at final review. |
| ASM-COKC-002 | This domain is separate from `operational-reporting-downloads` because it owns KPI semantics and SLA/control rules, not only report delivery. | `kpisquery.php`, `reportecontroladquery.php`, `reportecontrololquery.php` and `reporteseguimientoquery.php` encode KPI calculations. | Confirm ownership boundary. |
| ASM-COKC-003 | Capacity `200` and thresholds `1/2` days are candidate SLA rules. | `kpisquery.php` hardcodes these values for AP, DAM and REQ. | Confirm SLA catalog. |
| ASM-COKC-004 | KPI seed client ids `417`, `452`, `471` represent the scope of the inspected vehicular KPI logic. | `kpisquery.php` uses these ids when building temporary tables. | Confirm whether this should be session-scoped. |
| ASM-OEC-001 | The reconstructed domain name is `operational-expense-control`. | Evidence centers on operational expense reports by case, order, item/SKU and export control. | Confirm naming at final review. |
| ASM-OEC-002 | This domain is separate from `vehicle-cost-accounting-reporting` because it focuses on expense composition by operational document/item, not full vehicle cost accounting and ZDAM output. | `gastosquery.php`, `gastos-items.php`, `control-gastos.php` and `reporte-gastos-skuquery.php`. | Confirm ownership boundary. |
| ASM-OEC-003 | API-backed item expenses share the same business domain as legacy PHP expense reports. | UI labels and filters align around operational expenses, supplier, line, invoice and order. | Confirm API ownership. |
| ASM-OCDA-001 | The reconstructed domain name is `operational-case-dossier-access`. | Evidence centers on searching operational dossiers and accessing files/documents by folder/case. | Confirm naming at final review. |
| ASM-OCDA-002 | This domain is separate from `customs-document-approval` because it is document discovery/access after or alongside operations, not approval workflow. | `archivos.php`, `archivosquery.php`, `documentosotros.php`. | Confirm ownership boundary. |
| ASM-OCDA-003 | `tipo_usuario` controls document visibility in dossier access. | `documentosotros.php` branches by numeric session user type. | Confirm role catalog. |
| ASM-EAPT-001 | The reconstructed domain name is `external-agency-procedure-tracking`. | Evidence centers on SENASAG/external entity procedures, stages and states. | Confirm naming at final review. |
| ASM-EAPT-002 | `identidademisora=2` is interpreted as SENASAG in the observed report. | `senasag.php` defaults `identidademisora=2` and labels the report SENASAG. | Confirm catalog. |
| ASM-EAPT-003 | Current-stage filtering is inferred from selected stage date present and next stage date absent. | `senasagquery.php` builds `E_id IS NOT NULL` and next `E_id+1 IS NULL`. | Confirm workflow logic. |
| ASM-FMOT-001 | The reconstructed domain name is `form1-modification-observation-tracking`. | Evidence centers on Form1 modifications, observations, missing documents and call history. | Confirm naming at final review. |
| ASM-FMOT-002 | This domain is separate from `continuous-improvement-nonconformity` because it focuses on formal Form1/case corrections and observed documents, not RNC improvement lifecycle. | `modificacionesquery.php`, `observadasquery.php`, `historial_llamadas.php`. | Confirm ownership boundary. |
| ASM-FMOT-003 | Form1 EDP states `1`, `3` and `7` are interpreted as observation, ingress and conclusion milestones. | `modificacionesquery.php` uses these ids for `tmp_observacion`, `tmp_ingreso` and `tmp_concluido`. | Confirm official state catalog. |
| ASM-FMOT-004 | `permisoclientes=1` controls client-visible modification detail. | `modificacionesquery.php` filters subcontravention rows by this value. | Confirm visibility semantics. |
| ASM-VCTT-001 | The reconstructed domain name is `vehicle-chassis-timeline-trace`. | Evidence centers on visual bitacora by chassis, inventory milestones and evidence details. | Confirm naming at final review. |
| ASM-VCTT-002 | This domain is separate from `warehouse-inventory-reporting` because it presents an interactive per-chassis lifecycle trace rather than tabular warehouse reports. | `bitacora_chasis.php` implements a timeline and evidence modals. | Confirm ownership boundary. |
| ASM-VCTT-003 | The six timeline milestone ids are treated as stable candidate catalog values. | `revisionData` hardcodes type ids and labels. | Confirm inventory catalog. |
| ASM-VCTT-004 | `created_at` is interpreted as the date used to identify the latest milestone. | `bitacora_chasis.php` selects max `created_at`. | Confirm operational date semantics. |
| ASM-EPDP-001 | The reconstructed domain name is `executive-powerbi-dashboard-portal`. | Evidence centers on dashboard PHP pages embedding Power BI reports. | Confirm naming at final review. |
| ASM-EPDP-002 | This domain is separate from operational KPI/reporting because ASGARD mainly hosts external Power BI content here. | `dashboard*.php` files contain iframe containers rather than local calculations. | Confirm ownership boundary. |
| ASM-EPDP-003 | Power BI dataset logic, refresh and row-level security are external to the inspected repo. | No dataset/DAX/Power Query source appears in dashboard PHP files. | Locate BI workspace documentation. |
| ASM-NWP-001 | The reconstructed domain name is `nationalization-weekly-planning`. | Evidence centers on weekly planning upload, confirmation and chasis nationalization dates. | Confirm naming at final review. |
| ASM-NWP-002 | This domain is separate from vehicle import management because it plans nationalization execution dates after/around arrival, rather than owning the full import case lifecycle. | `planificacion-nacionalizacion.php` focuses on planning chasis and API confirmation. | Confirm ownership boundary. |
| ASM-NWP-003 | `dav_casos.fecha_planificacion_nacionalizacion` is treated as the candidate persisted planned date. | Schema includes this field and UI displays same named value. | Confirm API write path. |
| ASM-PLIV-001 | The reconstructed domain name is `packing-list-import-validation`. | Evidence centers on Excel import, validation and PDF generation of lista de empaque. | Confirm naming at final review. |
| ASM-PLIV-002 | This domain is separate from document-exchange/OCR because it parses structured logistics packing data and generates a specific operational PDF. | `uploadExcelListaEmpaque.php` writes structured `dav_listaempaque_*` rows. | Confirm ownership boundary. |
| ASM-PLIV-003 | Weight equality is interpreted as strict equality because the code uses direct `!==` / `!=` comparisons. | `uploadExcelListaEmpaque.php` validation block. | Confirm tolerance policy. |
| ASM-VRBU-001 | The reconstructed domain name is `vehicle-request-bulk-update`. | Evidence centers on Excel-based mass updates for vehicle request/case data. | Confirm naming at final review. |
| ASM-VRBU-002 | This domain is separate from `vehicle-import-management` because it is a corrective bulk-maintenance workflow over existing vehicle/case records. | `updateSolicitudVehiculos.php`, `VehiculosClass.php`. | Confirm ownership boundary. |
| ASM-VRBU-003 | Merchandise id `34` is interpreted as the vehicle chasis-bearing merchandise type for this workflow. | `buscarChasisEnCarpeta` filters `dav_partidas.idmercancia=34`. | Confirm catalog. |
| ASM-IVBC-001 | The reconstructed domain name is `inventory-vin-billing-control`. | Evidence centers on VIN/chasis billing precalculation, confirmation and period tables. | Confirm naming at final review. |
| ASM-IVBC-002 | This domain is separate from receivables because it determines billable inventory VIN units before or alongside formal invoice/receivable generation. | `facturacion-inventario.php` has precalculation/confirmation, not invoice issuance. | Confirm accounting boundary. |
| ASM-IVBC-003 | Day 21 to day 20 is treated as the monthly VIN billing cycle. | Date logic in `facturacion-inventario.php`. | Confirm commercial policy. |
| ASM-BDRC-001 | The reconstructed domain name is `billing-document-reception-confirmation`. | Evidence centers on confirming reception of sent planillas, facturas, notas de cobranza and cites. | Confirm naming at final review. |
| ASM-BDRC-002 | This domain is separate from billing receivables because it manages reception acknowledgement of delivered fiscal/billing/support documents, not invoice creation or collection. | `recepcionplanillas.php` and AJAX update reception flags only. | Confirm accounting boundary. |
| ASM-BDRC-003 | Cutoff date `2021-08-02` is treated as an observed historical start/filter. | SQL queries in `recepcionplanillas_ajax.php` filter dates greater/equal to this date. | Confirm whether it is migratory, temporary or policy. |
| ASM-BIPD-001 | The reconstructed domain name is `billing-invoice-planilla-document-generation`. | Evidence centers on generating/downloading PDF factura-planilla documents with fiscal QR/control data and letterhead. | Confirm naming at final review. |
| ASM-BIPD-002 | This domain is separate from receivables because it owns document rendering/delivery mechanics, while receivables owns payment, saldo and collection semantics. | `generarfacturaplanillacliente.php`, `descargarfactura.php`, `descargarplanilla.php`. | Confirm ownership boundary. |
| ASM-BIPD-003 | `iddosificacion <= 39` is interpreted as the legacy/offline document path. | `descargarfactura.php` sets `enlinea=0` for this condition. | Confirm fiscal transition rule. |
| ASM-ASRM-001 | The reconstructed domain name is `additional-services-request-management`. | Evidence centers on Servicios Adicionales request creation, tramite selection, state queues and document exchange integration. | Confirm naming at final review. |
| ASM-ASRM-002 | This domain is separate from advisory-management-services because it focuses on additional-service intake and workflow attached to shipment/GA contexts, including exchange document setup. | `logistica/frames/servicios-adicionales.php`, `tbl-estados.js`, `servicios-adicionales.js`. | Confirm ownership boundary. |
| ASM-ASRM-003 | State ids `0` through `8` are interpreted from schema/views as pending through facturado. | `ages_solicitudes_asesoria_gestion.estado`, `v_reporte_general_asesoria_gestion`. | Confirm official state catalog. |
| ASM-BREI-001 | The reconstructed domain name is `bulk-request-excel-import-validation`. | Evidence centers on Excel template generation, upload, staging, row validation and lot commit. | Confirm naming at final review. |
| ASM-BREI-002 | This domain is separate from customs-request-intake because it owns bulk import validation/staging behavior, while request intake owns broader single-request creation and send/finalization. | `uploadExcelSolicitud.php`, `formatoSolicitudMasiva.php`, `SolicitudClass.php`. | Confirm ownership boundary. |
| ASM-BREI-003 | The upload behaves as all-or-nothing because any row error prevents creating every row in the lot. | `uploadExcelSolicitud.php` checks `$error==0` before looping `crearGestionAduanera`. | Confirm desired business policy. |
| ASM-BSQI-001 | The reconstructed domain name is `bulk-shipment-quotation-import`. | Evidence centers on logistics Excel upload creating quotation/shipment records per row. | Confirm naming at final review. |
| ASM-BSQI-002 | This domain is separate from logistics quotation costing because it owns bulk import and row-to-shipment creation, not quote calculation/rating itself. | `uploadExcelCargaMasiva.php`, `CargaMasiva.php`. | Confirm ownership boundary. |
| ASM-BSQI-003 | The observed import appears permissive: unresolved catalogs can be set to `0` while the response still returns success. | `uploadExcelCargaMasiva.php` and `CargaMasiva.php`. | Confirm intended validation policy. |
| ASM-CDCRA-001 | The reconstructed domain name is `customs-dav-client-review-approval`. | Evidence centers on DEMIS DAV/FDM review screens and AJAX endpoints. | Confirm naming at final review. |
| ASM-CDCRA-002 | This domain is separate from customs-document-approval because it manages client decision and closure of DAV/FDM declarations, not generic document upload/approval. | `componentes/demis/*`, `ajax/demis/*`, `DemisClass.php`. | Confirm ownership boundary. |
| ASM-CDCRA-003 | `idestadocliente` values `1`, `2` and `9` are interpreted as APROBADO, RECHAZADO and Para Revision. | Display expressions in `DemisClass.php`. | Confirm official state catalog. |
| ASM-CDCRA-004 | EDP state `14` is interpreted as the client DAV/FDM review-follow-up event. | `finalizarRevision.php` registers `idestadoedp=14` for each closed DAV. | Confirm EDP catalog meaning. |
| ASM-LSFC-001 | The reconstructed domain name is `logistics-shipment-finalization-control`. | Evidence centers on `finalizar-embarque` UI/AJAX and `fecha_finalizacion` write blocking. | Confirm naming at final review. |
| ASM-LSFC-002 | This domain is separate from logistics-order-status-milestones because it owns manual closure and post-closure locks, while EDP milestones own status history capture. | `finalizar-embarque.php`, `frames/*` checks. | Confirm ownership boundary. |
| ASM-LSFC-003 | Client `429` has special completion gates before shipment finalization. | `finalizar-embarque.php` validates EDP, costs and GA only under `idcliente == 429`. | Confirm client-specific policy. |
| ASM-LSFC-004 | EDP final states `53`, `160` and `99` are interpreted as client-specific closure states, subject to a possible implementation issue. | `finalizar-embarque.php:7-13`. | Confirm catalog and intended branching. |
| ASM-LRTAM-001 | The reconstructed domain name is `logistics-route-trip-assignment-management`. | Evidence centers on shipment routes, recovered trips and TCK assignment updates. | Confirm naming at final review. |
| ASM-LRTAM-002 | This domain is separate from logistics-shipment-tracking because it manages planned/assigned routes and trip links, not event tracking and location history. | `logis_embarquesrutas`, `tck_asignacion_viaje.embarque_id`. | Confirm ownership boundary. |
| ASM-LRTAM-003 | Permission report id `69` is interpreted as route-management write permission. | `embarques_rutas.php` and `listaRutas.php` query `idreportescliente = 69`. | Confirm permission catalog. |
| ASM-LRTAM-004 | Deleting a trip from the shipment view is interpreted as logical removal from operational use, not necessarily cancellation in TCK. | `eliminarViaje` sets `deleted_at`/`deleted_by` on `tck_asignacion_viaje`. | Confirm TCK semantics. |
| ASM-SCRM-001 | The reconstructed domain name is `shipment-customs-request-management`. | Evidence centers on GA creation/edit/send from the logistics shipment customs tab. | Confirm naming at final review. |
| ASM-SCRM-002 | This domain is separate from customs-request-intake because it is the shipment-embedded operational variant with route/closure dependencies and GA-specific edit/send behavior. | `embarques_ver_gestion_aduanera.php`, `guardarGestionAduanera.php`, `SolicitudesClass.php`. | Confirm ownership boundary. |
| ASM-SCRM-003 | Permission report id `65` is interpreted as customs-management write permission within logistics. | `embarques_ver_gestion_aduanera.php` queries `idreportescliente = 65`. | Confirm permission catalog. |
| ASM-SCRM-004 | Cliente `429` is inferred to allow only one GA per embarque from this UI. | Button is hidden when count of `dav_casosprevios` by `idembarquelogis` is at least one. | Confirm client policy. |
| ASM-LDDM-001 | The reconstructed domain name is `legacy-dispatch-document-maintenance`. | Evidence centers on `despachover.php` and `despachoajax.php`. | Confirm whether this legacy module is still active. |
| ASM-LDDM-002 | This domain is separate from document-exchange-ocr because it writes legacy dispatch/document tables and filesystem paths directly, without exchange templates/OCR. | `despachoajax.php`, `despachover.php`. | Confirm ownership boundary. |
| ASM-LDDM-003 | Cliente `417` is treated as hardcoded module scope. | `despachover.php` assigns `$idcliente = '417'`. | Confirm if module is customer-specific or obsolete. |
| ASM-LDDM-004 | Missing DDL for `logis_despachos` and `logis_documentos` is treated as a baseline/schema gap, not proof the tables never exist. | `.data_base/asgard.sql` search found no definitions. | Validate production schema. |
| ASM-EMDPRC-001 | The reconstructed domain name is `export-mic-dex-physical-reception-control`. | Evidence centers on MIC/DEX physical reception UI and `dex_suma` state-date updates. | Confirm naming at final review. |
| ASM-EMDPRC-002 | This domain is separate from transport-export-tracking because it owns document physical-state changes and history, not trip/event tracking. | `dex_suma`, `dex_suma_estado_historial`, `ActualizarMICs.php`. | Confirm ownership boundary. |
| ASM-EMDPRC-003 | `ASGARD_TYPE` values are interpreted as provider/client actor split for transition rules. | `ActualizarMICs.php` maps `PROVEEDORES` to `1`, otherwise `2`. | Confirm deployment contexts. |
| ASM-EMDPRC-004 | Estado documental is inferred from date precedence: concluido, enviado, recibido, pendiente. | `RecepcionFisicaMICs.php` derives label with nested IF. | Confirm official lifecycle. |
| ASM-ATDC-001 | The reconstructed domain name is `alicorp-transit-deadline-control`. | Evidence centers on Alicorp control query, automatic vencimiento update and transit-closure display. | Confirm naming and business ownership. |
| ASM-ATDC-002 | Cliente `775` is interpreted as Alicorp scope. | `control_alicorpquery.php` filters `dav_casos.idcliente=775` and cancelled invoices for the same id. | Confirm client catalog. |
| ASM-ATDC-003 | `alicorp_vencimiento = fechavalidaciondui + 60 days` is treated as a business deadline rule. | The query updates missing deadline with `DATE_ADD(..., INTERVAL 60 DAY)`. | Confirm source and legal/contractual meaning. |
| ASM-ATDC-004 | `DATEDIFF <= 5` without DEX exit is interpreted as a near-deadline alert. | `error_vencimiento` expression in `control_alicorpquery.php`. | Confirm SLA/alert threshold. |
| ASM-CTLRC-001 | The reconstructed domain name is `customs-tax-liquidation-return-confirmation`. | Evidence centers on Detalle de Items confirmation and `fecharetornoliquidacion`. | Confirm naming and ownership. |
| ASM-CTLRC-002 | `fecharetornoliquidacion` is interpreted as confirmation that reviewed values returned/approved and tax payment may proceed. | Email body says values are reviewed and payment should proceed. | Confirm exact business meaning. |
| ASM-CTLRC-003 | `dav_retornomailsliquidacion` is interpreted as the configured recipient list for this confirmation. | Query joins by case client and city. | Confirm recipient governance. |
| ASM-LSCCC-001 | The reconstructed domain name is `logistics-shipment-cost-capture-control`. | Evidence centers on shipment costs tab and `logis_costos` persistence. | Confirm naming and ownership boundary. |
| ASM-LSCCC-002 | Permission/report id `70` is interpreted as write permission for shipment costs. | `frames/costos.php` checks `idreportescliente = 70`. | Confirm permission catalog. |
| ASM-LSCCC-003 | Category id `6` is interpreted as merchandise/automatic cost category. | `CostosClass::costosInternos($embarqueId, 6)`. | Confirm category catalog. |
| ASM-LSCCC-004 | Save behavior is interpreted as full replacement of active costs, not incremental patch. | `deleteOldCost` runs before inserting submitted data. | Confirm intended versioning policy. |
| ASM-LSDAM-001 | The reconstructed domain name is `logistics-shipment-document-attachment-management`. | Evidence centers on shipment documents tab, local attachments and exchange handoff. | Confirm naming and ownership. |
| ASM-LSDAM-002 | Complete exchange is inferred when both `idExchange_as` and `idExchange_id` are present. | `frames/documentos.php` sets `$tiene_intercambio`. | Confirm exchange-link semantics. |
| ASM-LSDAM-003 | Local document mode is interpreted as fallback when exchange is missing. | Same file lists `logis_embarquesdocumentos` only when no exchange. | Confirm whether fallback is legacy or active policy. |
| ASM-LSQD-001 | The reconstructed domain name is `logistics-shipment-quotation-duplication`. | Evidence centers on duplicate quotation and duplicate shipment actions. | Confirm naming and boundary. |
| ASM-LSQD-002 | `logis_embarques.cotizacion` differentiates quotation (`1`) from shipment (`0`) in duplicated records. | Duplicate endpoints set the flag explicitly. | Confirm lifecycle semantics. |
| ASM-LSQD-003 | Duplicated records are expected to be reviewed/updated manually after creation. | UI warning text states duplicated shipment reuses same information. | Confirm whether review should be persisted. |
| ASM-LOIDM-001 | The reconstructed domain name is `logistics-order-item-detail-maintenance`. | Evidence centers on Items view and `saveDescripcionItems.php`. | Confirm naming and ownership. |
| ASM-LOIDM-002 | The endpoint is interpreted as maintenance of item fields, not full order lifecycle. | It only updates `logis_pedidos_detalle` fields from submitted inputs. | Confirm scope. |
| ASM-VIDBU-001 | The reconstructed domain name is `vehicle-invoice-data-bulk-update`. | Evidence centers on the `Fechas Facturas` GA tab, Excel template generation and multi-table invoice updates by chasis. | Confirm naming and ownership. |
| ASM-VIDBU-002 | This domain is separate from general vehicle import management because it is a corrective bulk update over existing shipment/customs vehicle invoice data. | `fechasfacturas.php` updates existing vehicle/case/factura/DAM rows instead of creating the import lifecycle. | Confirm boundary. |
| ASM-VIDBU-003 | A non-empty `idsuma` means the row is locked against invoice-date update from this flow. | UI observation says `YA NO ES POSIBLE ACTUALIZAR LA FECHA` when `idsuma` is present. | Confirm SUMA/DAM locking policy. |
| ASM-VIDBU-004 | Positive uploaded `monto` is interpreted as the trigger to recalculate FOB, freight, insurance, CIF and downstream DAM/DAV values. | The recalculation block runs only when `(float)monto > 0`. | Confirm financial recalculation policy. |
| ASM-AOBSI-001 | The reconstructed domain name is `alicorp-ocr-bulk-shipment-intake`. | Evidence centers on OCR mass upload that creates shipments, customs requests and document exchange links. | Confirm naming and ownership. |
| ASM-AOBSI-002 | This domain is separate from `document-exchange-ocr` because it uses OCR results to create logistics/customs business entities, not only read/validate documents. | `get-ocr-alicorp-masivo.php` calls shipment and GA creation flows after OCR. | Confirm boundary. |
| ASM-AOBSI-003 | `sinData` is interpreted as a partial-creation state: shipment can exist while GA must be completed manually. | GA creation is inside `if(!$sinData)`. | Confirm operational recovery. |
| ASM-AOBSI-004 | `cargar_servicios` values are interpreted as candidate additional services to create or link after GA creation. | JS passes the array to `actualizaridexchange.php`. | Confirm final service creation behavior. |
| ASM-SCIRS-001 | The reconstructed domain name is `shipment-commercial-invoice-reference-sync`. | Evidence centers on syncing `FACTURA COMERCIAL` document reference codes into `logis_embarques.facturacomercial`. | Confirm naming and ownership. |
| ASM-SCIRS-002 | This is treated as a narrow logistics/document integration domain because it mutates shipment business data from document metadata. | `embarquereferencia.php` updates `logis_embarques`. | Confirm whether to merge later with document attachment/exchange domain. |
| ASM-LSEPS-001 | The reconstructed domain name is `logistics-shipment-edit-participant-sync`. | Evidence centers on edit controller/class methods that update shipment data and document participants. | Confirm naming and boundary. |
| ASM-LSEPS-002 | Quotation edit and shipment edit are treated as variants of one edit domain because they share controller and child replacement logic. | `actualizarEmbarque` changes operator behavior inside the same method. | Split later if ownership differs. |
| ASM-LSEPS-003 | Participant sync is considered part of the edit domain because it is executed immediately after local edit success. | `editarParticipantesIntercambio` is called by `editarCotizacionCliente`. | Confirm exchange ownership. |
| ASM-VEIV-001 | The reconstructed domain name is `vehicle-excel-intake-validation`. | Evidence centers on `vehiculos.php` and `CargaVehiculos.php` uploading, persisting and validating vehicle rows. | Confirm naming and ownership. |
| ASM-VEIV-002 | This domain is separate from `vehicle-import-management` because it owns the Excel intake/validation lifecycle, while vehicle import management owns broader vehicle request operations. | Dense upload, parser, validation and error-state logic in vehicle Excel files. | Confirm boundary. |
| ASM-VEIV-003 | Error values `1`, `2` and `3` are interpreted as blocking error, catalog inclusion/review and catalog modification respectively. | UI/mail text separates include/modify database cases; validation sets numeric errors. | Confirm official error catalog. |
| ASM-CDOVU-001 | The reconstructed domain name is `customs-dex-ocr-validation-update`. | Evidence centers on DEX OCR updating `dav_casos` declaration/Sidunea/date fields and returning comparison observations. | Confirm naming and ownership. |
| ASM-CDOVU-002 | This domain is separate from generic document OCR because it mutates customs declaration fields from OCR output. | `documento-ocr-dex.php` runs `UPDATE dav_casos` after OCR. | Confirm boundary with document-exchange-ocr. |
| ASM-CDOVU-003 | Folder equality between OCR `carpeta` and ASGARD `carpeta` is interpreted as the business gate for applying DEX updates. | Updates are inside the matching-folder branch. | Confirm whether additional gates exist. |
| ASM-AAOPR-001 | The reconstructed domain name is `alicorp-albo-ocr-payment-reconciliation`. | Evidence centers on `lectura-ocr-falbo.php` matching OCR invoice data to payments and debit notes. | Confirm naming and ownership. |
| ASM-AAOPR-002 | This domain is separate from `alicorp-transit-deadline-control` because it reconciles payment/debit-note data, while the transit domain consumes or reports the closure flag. | `lectura-ocr-falbo.php` updates `dav_pagosdetalle` and `dav_notasdebitodetalle` in addition to `alicorp_cierre_transito`. | Confirm boundary. |
| ASM-AAOPR-003 | Concept `272` is interpreted as the ALBO/FALBO payment concept for this flow. | `$idconcepto=272` is fixed in the endpoint. | Confirm official concept catalog. |
| ASM-LBPOC-001 | The reconstructed domain name is `logistics-bl-policy-ocr-capture`. | Evidence centers on `lectura-ocr-bl.php` persisting BL/policy OCR into `logis_lecturablpoliza`. | Confirm naming and owner. |
| ASM-VSILC-001 | The reconstructed domain name is `vehicle-sales-invoice-ocr-ledger-capture`. | Evidence centers on Imcruz invoice OCR inserting `logis_libroventas`. | Confirm whether ownership is logistics, finance or vehicle operations. |
| ASM-DSPC-001 | The reconstructed domain name is `document-service-plan-ocr-capture`. | Evidence centers on `lectura-ocr.php` replacing `dav_planillasdp` readings for a document. | Confirm business label for planilla DP. |
| ASM-ASOPR-001 | The reconstructed domain name is `alicorp-supplier-ocr-payment-reconciliation`. | Evidence centers on SENAVEX/FDAB/Jennefer OCR updating payments, debit notes and transit closure. | Confirm whether to merge with ALBO/FALBO domain later. |
| ASM-ASOPR-002 | SENAVEX/FDAB/Jennefer are grouped because the same payment reconciliation pattern repeats with provider-specific models and concepts. | Shared context/payment/update structure across endpoints. | Confirm provider-specific ownership. |
| ASM-CDDSDC-001 | The reconstructed domain name is `customs-dam-document-send-date-control`. | Evidence centers on `documento-dam.php` setting `dav_facturacomercial.fechaenviodam` from a DAM document event. | Confirm ownership and naming. |
| ASM-CDDSDC-002 | This is separate from general customs/vehicle reporting because it is a document-triggered mutation of the DAM send milestone. | It writes `fechaenviodam`, which other controls consume. | Confirm whether to merge with customs-document-approval later. |
| ASM-CSRF-001 | The reconstructed domain name is `customer-shipment-rating-feedback`. | Evidence centers on the rating module saving customer ratings for shipment/request/case context. | Confirm naming and product ownership. |
| ASM-CSRF-002 | The 30-day rule is interpreted as a monthly feedback prompt. | `verificaMensual` compares last `created_at + 30 DAY` with current date. | Confirm cadence and UX policy. |
| ASM-CPR-001 | The reconstructed domain name is `customer-password-recovery`. | Evidence centers on `resetpassword` endpoints and `dav_reseteos_passswords_clientes`. | Confirm naming and security ownership. |
| ASM-CPR-002 | This domain is separate from `identity-access` because it handles forgotten-password recovery outside the MFA login challenge. | `login.php` links to `resetpassword/index.php`; reset endpoints update credential state. | Confirm boundary with identity/access management. |
| ASM-CPR-003 | Token expiration is interpreted as same-calendar-day validity. | `verifyToken` compares token creation date with `date('Y-m-d')`. | Confirm intended TTL. |
| ASM-VRPODG-001 | The reconstructed domain name is `vehicle-reception-part-ocr-document-generation`. | Evidence centers on OCR PR creating `ocr_parte_recepcion` and `dav_documentos` type `71`. | Confirm naming and operations owner. |
| ASM-VRPODG-002 | Document type `71` is interpreted as document 901 / Parte de Recepcion evidence. | `OcrUtil` names `$documento901 = 71` and creates `dav_documentos`. | Confirm official document catalog. |
| ASM-VRPODG-003 | Client ids `417` and `755` are interpreted as the current eligible scope for this OCR flow. | `lectura-ocr-pr.php` hardcodes those ids. | Confirm rollout scope. |
| ASM-CPLSA-001 | The reconstructed domain name is `customer-primary-login-session-audit`. | Evidence centers on `veriflogin.php` validating primary login and writing access audit. | Confirm naming with security owner. |
| ASM-CPLSA-002 | This domain is separate from `identity-access` because `identity-access` currently documents the MFA challenge after primary credential acceptance. | `veriflogin.php` redirects to `2fa.php`; MFA logic lives under `2fa/`. | Confirm whether to merge later. |
| ASM-CPLSA-003 | Master password use is interpreted as an operational bypass that must remain audit-distinguishable. | Login success log uses `inicio_sesion_master` identifier when master password validates. | Confirm policy and permitted roles. |
| ASM-TPTDO-001 | The reconstructed domain name is `third-party-token-document-onboarding`. | Evidence centers on aduana, seguro and gestor de transporte token/document/contact flows. | Confirm naming and ownership. |
| ASM-TPTDO-002 | This is separated from `master-data-configuration` because it focuses on external token completion and document onboarding, not generic catalog maintenance. | `formInfo.php?q={token}` and `enviarMail` flows expose third-party completion. | Confirm merge/split decision later. |
| ASM-TPTDO-003 | Agente tracking is included only as a simple related master, not as a tokenized onboarding variant. | `agenteTrackingClass.php` shows CRUD without token/email flow. | Confirm if tracking has another onboarding path. |
| ASM-AODPD-001 | The reconstructed domain name is `alicorp-operational-document-package-dispatch`. | Evidence centers on `cron/documentacionAlicorp.php` packaging and mailing operational documents. | Confirm naming and owner. |
| ASM-AODPD-002 | Customers `755` and `775` are interpreted as Alicorp/IASA scope for this cron. | The cron hardcodes `$clientes = array(775,755)`. | Confirm official client mapping. |
| ASM-AODPD-003 | `embarque_documentos_enviados` is interpreted as the idempotency/reprocess prevention marker. | Cases are selected with null marker and updated after send. | Confirm re-send policy. |
| ASM-VSPOCR-001 | The reconstructed domain name is `vehicle-soat-pdf-ocr-splitting`. | Evidence centers on `dividir-pdf-soat.php` splitting/OCRing SOAT PDFs by chassis. | Confirm naming and vehicle operations owner. |
| ASM-VSPOCR-002 | The generated per-chassis PDFs are interpreted as temporary artifacts for later manual/documental use. | The endpoint writes files under `/datadrive1/temporales/soat` but does not attach them to cases. | Confirm downstream workflow. |
